[project]
name = "vamisec-phishing-backend"
version = "0.1.0"
description = "FastAPI backend for VamiSec Phishing Radar."
requires-python = ">=3.10"
dependencies = [
  "fastapi==0.111.0",
  "httpx==0.27.0",
  "python-dotenv==1.0.1",
  "uvicorn==0.30.1",
]

[tool.uv]
dev-dependencies = []
